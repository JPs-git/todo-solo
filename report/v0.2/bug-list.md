# Todo-List v0.2 Bug 列表

## 概述
- **文档版本**：v1.0
- **生成时间**：2026-03-09 11:15
- **测试环境**：
  - 浏览器：Chromium
  - 应用地址：http://localhost:5174

## Bug 列表

| Bug 编号 | 标题 | 严重程度 | 状态 | 发现时间 | 修复时间 | 备注 |
|---------|------|----------|------|----------|----------|------|
| #001 | 连续添加任务时第二个任务输入框被禁用 | 高 | 未修复 | 2026-03-09 | - | 第二个任务的输入框被禁用，显示第一个任务的值 |

## Bug 详细信息

### Bug #001：连续添加任务时第二个任务输入框被禁用

**描述**：
连续添加两个新任务，第二个新任务无法修改任务标题等任何内容，也无法点击添加任务按钮。

**复现步骤**：
1. 点击添加新任务按钮
2. 输入任务名称（如"第一个任务"）
3. 点击添加任务按钮，成功添加
4. 再次点击添加新任务按钮
5. 问题出现：所有输入显示禁用，无法点击添加任务按钮

**期望结果**：
可以连续添加任意个任务。

**实际结果**：
- 第二次点击"添加新任务"按钮后，表单中的输入框被禁用
- 输入框显示的是第一个任务的值（`value="第一个任务"`）
- 添加任务按钮处于禁用状态，无法点击

**测试验证**：
- 测试脚本：`e2e/bug-verify.spec.ts`
- 测试结果：失败（Bug 确认）
- 关键日志：
  ```
  第二个任务输入框是否可用: false
  输入框元素属性: disabled, value="第一个任务"
  ```

**可能原因**：
1. 表单状态未重置：添加第一个任务后，表单状态（如 `editingTask`）未被正确重置
2. 输入框缓存问题：第二次打开表单时，输入框可能保留了第一次的值和禁用状态
3. React 状态管理问题：`TaskForm` 组件可能在第一次提交后未正确卸载或重置

**建议排查方向**：
1. 检查 `App.tsx` 中 `editingTask` 状态的管理
2. 检查 `TaskForm` 组件在关闭后是否正确重置内部状态
3. 检查是否存在表单缓存或状态未清理的问题

**相关文件**：
- 测试脚本：`e2e/bug-verify.spec.ts`
- 错误截图：`test-results/bug-verify-BUG验证.../test-failed-1.png`
- 测试报告：`report/v0.2/e2e-test-report.md`

## 修复状态跟踪

| Bug 编号 | 修复状态 | 修复负责人 | 修复日期 | 验证状态 | 验证日期 |
|---------|----------|------------|----------|----------|----------|
| #001 | 未修复 | - | - | - | - |

## 备注
- 此文档用于跟踪 v0.2 版本中发现的所有 bug
- 每次测试发现新 bug 时，应更新此文档
- 修复 bug 后，应更新修复状态和相关信息
